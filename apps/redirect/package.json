{
  "name": "redirect",
  "version": "1.0.0",
  "description": "Redirects to the target URL",
  "main": "index.js",
  "scripts": {
    "tsc": "tsc",
    "build": "tsc && copyfiles package.json build/ && cd build/ && npm install --production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Mansour Kalagar",
  "license": "ISC",
  "dependencies": {
    "amazon-dax-client": "^1.2.9",
    "aws-sdk": "^2.1613.0",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.137",
    "@types/node": "^20.12.8",
    "copyfiles": "^2.4.1"
  }
}
